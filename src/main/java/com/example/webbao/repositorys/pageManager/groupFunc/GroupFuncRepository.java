package com.example.webbao.repositorys.pageManager.groupFunc;

import com.example.webbao.models.pageManager.groupFunc.GroupFunc;
import com.example.webbao.models.pageManager.menu.MenuCore;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GroupFuncRepository extends JpaRepository<GroupFunc,Long> {
}
